---
import { Image } from 'astro:assets';
import Base from '../layouts/Base.astro';
import Button from '../components/Button.astro';
import Hours from '../components/Hours.astro';
import { contact, isDraft } from '../data/site';
import hero from '../assets/hoian-1.jpg';

const rise = 'opacity-0 animate-rise motion-reduce:animate-none motion-reduce:opacity-100';
const card = 'border border-night/15 bg-paper-deep/55 p-[clamp(1.75rem,3vw,2.5rem)] border-t-[3px]';
---

<Base
  title="Sushi & Wok"
  description="Sushi von der kalten Theke, Gerichte aus dem heißen Wok — asiatisch essen mitten in Löbau. Speisekarte, Öffnungszeiten und Reservierung."
>
  <section
    class="relative overflow-hidden bg-[radial-gradient(120%_90%_at_50%_-10%,var(--color-ink-700),var(--color-ink-900)_62%)] pt-[clamp(7rem,18vh,12rem)] pb-[clamp(4rem,10vh,7rem)] text-cream"
  >
    <!-- Die Laternen von Hoi An als Foto statt als CSS-Nachbau. Der Verlauf
         darüber hält die hellen Laternen von der Schrift fern. -->
    <Image
      src={hero}
      alt=""
      width={2560}
      height={1707}
      widths={[960, 1440, 1920, 2560]}
      sizes="100vw"
      loading="eager"
      fetchpriority="high"
      class="absolute inset-0 h-full w-full object-cover"
    />
    <div
      class="absolute inset-0 bg-linear-to-b from-ink-900/88 via-ink-900/75 to-ink-900/92"
    >
    </div>
    <div class="relative z-20 mx-auto w-full max-w-shell px-gutter text-center">
      <h1
        class={`font-display wonk-soft text-display leading-none tracking-[-0.03em] text-balance ${rise}`}
        style="animation-delay:.15s"
      >
        Hoi An
        <em
          class="mt-4 block text-[0.42em] font-normal tracking-[0.32em] text-wall uppercase not-italic wonk-none"
          >Sushi &amp; Wok</em
        >
      </h1>
      <p
        class={`mx-auto mt-7 mb-10 max-w-[44ch] text-lead leading-[1.5] text-pretty text-cream ${rise}`}
        style="animation-delay:.35s"
      >
        Auf der einen Seite wird Fisch geschnitten, auf der anderen brennt der Wok. Beides kommt an
        denselben Tisch.
      </p>
      <div class={`flex flex-wrap justify-center gap-3.5 ${rise}`} style="animation-delay:.5s">
        <Button href="/speisekarte/">Speisekarte ansehen</Button>
        <Button href={contact.phoneHref} variant="ghost">Tisch reservieren</Button>
      </div>
    </div>
  </section>

  <section class="py-band">
    <div class="mx-auto w-full max-w-shell px-gutter">
      <h2 class="max-w-[18ch] font-display wonk text-heading leading-none tracking-[-0.015em] text-balance">
        Zwei Küchen, ein Haus
      </h2>
      <p class="mt-6 mb-12 max-w-[62ch] text-lead leading-[1.55] text-pretty">
        Sushi und Wok brauchen das Gegenteil voneinander: die eine Hälfte kühlt, die andere heizt.
        Deshalb ist auch die Karte in zwei Hälften geteilt. Sie können in einer bleiben oder
        mischen.
      </p>

      <div class="grid gap-[clamp(1.5rem,4vw,3rem)] md:grid-cols-2">
        <div class={`${card} border-t-shutter`}>
          <h3 class="mb-3 font-display wonk text-title leading-none text-balance">Kalte Küche</h3>
          <p class="max-w-[46ch]">
            Roher Fisch, geformter Reis, kalte Vorspeisen. Wird an der Theke gemacht, nicht am Herd.
          </p>
        </div>
        <div class={`${card} border-t-lantern`}>
          <h3 class="mb-3 font-display wonk text-title leading-none text-balance">Heiße Küche</h3>
          <p class="max-w-[46ch]">
            Wok bei voller Hitze, Brühen über Stunden. Alles kommt heiß und in wenigen Minuten.
          </p>
        </div>
      </div>

      <p class="mt-10">
        <Button href="/speisekarte/" variant="outline">Ganze Karte öffnen</Button>
      </p>
    </div>
  </section>

  <section class="bg-wall py-band">
    <div class="mx-auto grid w-full max-w-shell gap-[clamp(2rem,5vw,4.5rem)] px-gutter md:grid-cols-2">
      <div>
        <h2 class="font-display wonk text-heading leading-none tracking-[-0.015em] text-balance">
          Ein Stück Hoi An in Löbau
        </h2>
      </div>
      <div>
        <p class="max-w-[62ch] text-lead leading-[1.55] text-pretty">
          Hoi An ist eine Hafenstadt in Zentralvietnam: Laternen über der Altstadt, offene
          Küchen in den Gassen. Die Stadt lässt sich nicht mitbringen, ihre Küche schon — dieselben
          Gewürze, derselbe Reis, dieselben langen Abende, an denen das Essen in die Mitte des
          Tisches kommt und alle zugreifen. Genau das steht hier an der Bahnhofstraße.
        </p>
        {
          isDraft && (
            <p class="mt-4 max-w-[62ch]">
              <strong class="font-semibold">TODO:</strong> Hier ein bis zwei Absätze über das Haus
              einsetzen — wer kocht, seit wann es das Restaurant gibt, was Ihnen beim Einkauf wichtig
              ist.
            </p>
          )
        }
      </div>
    </div>
  </section>

  <section class="bg-ink-900 py-band text-cream">
    <div class="mx-auto grid w-full max-w-shell gap-[clamp(2rem,5vw,4.5rem)] px-gutter md:grid-cols-2">
      <div>
        <h2 class="font-display wonk text-heading leading-none tracking-[-0.015em] text-balance">
          Öffnungszeiten
        </h2>
        <Hours tone="dark" class="mt-8" />
      </div>
      <div>
        <h2 class="font-display wonk text-heading leading-none tracking-[-0.015em] text-balance">Adresse</h2>
        <p class="mt-8 text-lead leading-[1.5] text-pretty">
          {contact.street}<br />
          {contact.postalCode} {contact.city}
        </p>
        <p class="mt-8">
          <Button href={contact.phoneHref}>Anrufen &amp; reservieren</Button>
        </p>
      </div>
    </div>
  </section>
</Base>
